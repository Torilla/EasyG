---

PlotDataManager:
  filesystem:
    - name: tabs
      children:
        - name: static_data
        - name: network_clients

  static_data:
    root: /tabs/static_data
    data_dir: "{tab_name}"
    data_file: data.dat
    plotitem_dir: plotitems
    plotitem_file: plotitem.obj

  network_clients: {}
